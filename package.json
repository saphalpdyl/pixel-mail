{
  "name": "email_clone",
  "version": "0.0.1",
  "description": "Fake Email Clone with JS , MySQL and Node",
  "repository": "https://github.com/saphalpdyl/Gmail_Clone-using-SQL.git",
  "author": "Saphal Poudyal",
  "scripts": {
    "install": "cd ./server/ && yarn install && cd ../client/ && yarn install",
    "test": "cd ./server/ && yarn run test && cd ../client/ && yarn run test",
    "lint": "cd ./server/ && yarn eslint . && cd ../client/ && yarn eslint ."
  },
  "license": "MIT",
  "private": "true",
  "devDependencies": {
    "eslint-config-google": "^0.14.0"
  }
}
